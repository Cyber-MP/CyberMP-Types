export interface worldNodeTransform {
  translation?: { x: number; y: number; z: number };
  rotation?: { i: number; j: number; k: number; r: number };
  scale?: { x: number; y: number; z: number };
}

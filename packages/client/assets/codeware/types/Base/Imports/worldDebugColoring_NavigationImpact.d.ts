export class worldDebugColoring_NavigationImpact extends worldEditorDebugColoringSettings {
  walkable?: Color;
  ignored?: Color;
  blocking?: Color;
  road?: Color;
  drones?: Color;
  terrain?: Color;
}

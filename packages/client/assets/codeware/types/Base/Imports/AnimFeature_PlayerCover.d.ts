export class AnimFeature_PlayerCover extends AnimFeature {
  cameraPositionMS?: { x: number; y: number; z: number; w: number };
  coverState?: number;
  leanAmount?: number;
  cameraOffsetAmount?: number;
  autoCoverActivationFrame?: boolean;
}
